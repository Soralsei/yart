name: Ubuntu

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  BUILD_TYPE: Release
  INSTALL_LOCATION: .local

jobs:
  build:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]') && !contains(github.event.head_commit.message, '[ci skip]')"

    steps:
      - uses: actions/checkout@v4

      - name: cache dependencies
        uses: actions/cache@v4
        id: cache
        with:
          path: ${{ github.workspace }}/${{ env.INSTALL_LOCATION }}
          key: ${{ runner.os }}-dependencies

      - name: install GoogleTest
        if: ${{ steps.cache.output.cache-hit != 'true' }}
        run: |
          cd ..
          git clone  --depth 1 --branch v1.14.0 https://github.com/google/googletest.git
          cd googletest
          cmake -Bbuild -DCMAKE_INSTALL_PREFIX=$GITHUB_WORKSPACE/$INSTALL_LOCATION -DCMAKE_COMPILE_WARNING_AS_ERROR=Off
          cmake --build build --config Release
          cmake --build build --target install --config Release

      - name: install glm
        if: ${{ steps.cache.output.cache-hit != 'true' }}
        run: |
          cd ..          

          git clone --depth 1 --branch 1.0.2 https://github.com/g-truc/glm.git
          cd glm

          cmake \
            -DGLM_BUILD_TESTS=OFF \
            -DBUILD_SHARED_LIBS=OFF \
            -B build . \
            -DCMAKE_INSTALL_PREFIX=$GITHUB_WORKSPACE/$INSTALL_LOCATION
          cmake --build build -- all
          cmake --build build -- install

          cd ../yart

      - name: configure
        run: cmake -Bbuild -DCMAKE_INSTALL_PREFIX=$GITHUB_WORKSPACE/$INSTALL_LOCATION -DYart_ENABLE_CODE_COVERAGE=1

      - name: build
        run: cmake --build build --config $BUILD_TYPE -j4

      - name: run tests
        run: |
          cd build
          ctest -C $BUILD_TYPE -VV

      - name: Code coverage using Codecov
        run: bash <(curl -s https://codecov.io/bash)

      - name: install project
        run: cmake --build build --target install --config Release
